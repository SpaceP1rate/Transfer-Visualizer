node_modules/
dist/
.DS_Store
*.log

# Local scratch
shot*.mjs
net.mjs

# Raw solver output. Reduce it with scripts/reduce_solutions.mjs and commit the
# CSVs it writes; the .mat files themselves are hundreds of megabytes per family
# pair and stay on disk.
edges_*/
**/edges_*/
site_data/

# MATLAB sources stay with the research code. The catalog is committed only in
# its converted form (public/data/orbits); edge_*.mat cannot be read outside
# MATLAB anyway — see scripts/export_edges.m.
*.mat

# Scratch: device_bash cannot delete, so discards land here.
_to_delete/
